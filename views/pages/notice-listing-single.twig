{% extends 'views/layouts/base.twig' %}

{% block content %}

<section class="notice-listing-single">

  {% if post.thumbnail %}
    <div class="notice-listing-single__image">
      <img src="{{ post.thumbnail.src }}" alt="{{ post.thumbnail.alt }}">
    </div>
  {% endif %}

  <div class="notice-listing-single__header">
    
    <h2 class="notice-listing-single__title">{{ post.title|esc_html }}</h2>
  </div>

  <div class="notice-listing-single__content">
    {{ post.content }}
  </div>

</section>

{% endblock %}